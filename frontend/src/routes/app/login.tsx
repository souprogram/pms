import { LoginForm } from "../../components/app/login-form";

export default function LoginPage() {
  return (
    <div className="flex h-dvh items-center justify-center bg-stone-50">
      <LoginForm className="w-xl max-w-xl" />
    </div>
  );
}
